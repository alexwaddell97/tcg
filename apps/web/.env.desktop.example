# Copy to .env.desktop.local to enable multiplayer in bundled desktop builds.
# Blank defaults to offline practice. Never put credentials in VITE_ variables.
VITE_SERVER_URL=
# Local testing: http://localhost:3001
# Distribution: https://your-game-server.example.com
