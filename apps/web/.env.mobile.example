# Copy to .env.mobile.local. Blank means local practice only.
# This value is public and bundled into the app; never put credentials here.
VITE_SERVER_URL=
# For multiplayer, use an HTTPS backend reachable from the phone:
# VITE_SERVER_URL=https://your-game-server.example.com
